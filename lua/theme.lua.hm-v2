-- Safe-Mode Theme (Minimal)
hl.config({
    general = {
        gaps_in = 5,
        gaps_out = 10,
        border_size = 2,
        layout = "dwindle",
    },
    decoration = {
        rounding = 10,
    },
    animations = {
        enabled = false, -- Disable animations for stability during debug
    }
})
